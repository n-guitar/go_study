package main

import "testing"

func TestArithmetic(t testing.T) {
	for _,test := range []struct{
		x,y float64
		want float64
	}{
		{1,1,2:0:1:1.000000e+00},
	}{
		if Arithmetic(test.x,test.y) != test.
	}
}
